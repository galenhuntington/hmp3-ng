on: [push]
name: build
jobs:
  stack:
    runs-on: 'ubuntu-latest'
    name: Stack
    steps:
      - uses: actions/checkout@v6
      - uses: haskell-actions/setup@v2
        with:
          enable-stack: true
      - run: stack test

