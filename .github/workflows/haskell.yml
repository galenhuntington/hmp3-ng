on: [push]
name: build
jobs:
  slim:
    runs-on: 'ubuntu-slim'
    name: GHC on Ubuntu Slim
    steps:
      - uses: actions/checkout@v4
      - uses: haskell-actions/setup@v2
      - run: cabal build all
      - run: cabal test all --test-show-details=streaming

